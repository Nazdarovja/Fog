@startuml

title Activity Diagram - TO-BE \n
|Kunde|
start
:Forespørgsel på hjemmeside;
|Quickbuild|
:Forespørgsel registreres;
|Sælger/Fog|
:Åbner forespørgsel;

repeat
:Kontaker kunde (mail/tlf);
|Kunde|
:Svarer på spørgsmål (mail/tlf);
|Sælger/Fog|
:Modtager svar (mail/tlf);
|Quickbuild|
:Opdaterer forespørgsel;
repeat while(Fyldestgørende info?) is (Nej)
|Quickbuild|
:Send tilbud til kunde (mail);

if(Accepterer tilbud?) then (Nej)
stop
else (Ja)
|Quickbuild|
:Konverterer tilbud til ordre;
|Sælger/Fog|
:Sender mail med ordrebekræftelse til kunde;
|Kunde|
:Betaler for ordre;
|Sælger/Fog|

if(Betaling for ordre modtaget?) then (Nej)
stop
else (Ja)
        :Frigiver/leverer ordre;
        |Kunde|
        :Modtager ordre;
stop

@enduml